// Déclarer un fonction
function plusUn(nombre) {
  return nombre + 1;
}

// Appeler une fonction
var resultat = plusUn(5);

// Déclarer une fonction dans ne variable
var plus2 = function(nombre) {
  return nombre + 2;
};
