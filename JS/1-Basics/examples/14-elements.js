var element = document.getElementById("monId");

// Recuperer le contenu de l'element
var contenu = element.inerHTML;

// Changer le contenu de l'element
element.innerHTML = "Nouveau contenu";

// Recuperer l'attribut d'un element
var id = element.id;
var classes = element.className;
var href = element.href;

// Recuperer la valeur d'un champs
var value = element.value;
